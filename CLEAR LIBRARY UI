local ClearLib = {}

function ClearLib:CreateWindow(UI_Name, Flt_Name)
    local sg = Instance.new("ScreenGui")
    sg.Name = "Custom_Library_UI"
    sg.Parent = game:GetService("CoreGui")
    sg.ResetOnSpawn = false

    local function MakeDraggable(frame)
        local dragToggle, dragStart, startPos
        frame.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragToggle = true
                dragStart = input.Position
                startPos = frame.Position
                input.Changed:Connect(function()
                    if input.UserInputState == Enum.UserInputState.End then dragToggle = false end
                end)
            end
        end)
        game:GetService("UserInputService").InputChanged:Connect(function(input)
            if dragToggle and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local delta = input.Position - dragStart
                frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
            end
        end)
    end

    -- === MAIN WINDOW (MIRIP FOTO) ===
    local Main = Instance.new("Frame", sg)
    Main.Size = UDim2.new(0, 420, 0, 260)
    Main.Position = UDim2.new(0.5, -210, 0.5, -130)
    Main.BackgroundColor3 = Color3.fromRGB(85, 230, 180) -- Tosca pas di foto
    Main.BorderSizePixel = 4
    Main.BorderColor3 = Color3.new(0, 0, 0)
    MakeDraggable(Main)

    local Head = Instance.new("Frame", Main)
    Head.Size = UDim2.new(1, 0, 0, 40)
    Head.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
    Head.BorderSizePixel = 3
    Head.BorderColor3 = Color3.new(0, 0, 0)
    
    local Title = Instance.new("TextLabel", Head)
    Title.Text = tostring(UI_Name):upper() -- Judul Custom
    Title.Size = UDim2.new(1, 0, 1, 0)
    Title.TextColor3 = Color3.new(1, 1, 1)
    Title.BackgroundTransparency = 1
    Title.Font = Enum.Font.SourceSansBold
    Title.TextSize = 22

    local Container = Instance.new("ScrollingFrame", Main)
    Container.Size = UDim2.new(1, -30, 1, -70)
    Container.Position = UDim2.new(0, 15, 0, 55)
    Container.BackgroundTransparency = 1
    Container.BorderSizePixel = 0
    Container.CanvasSize = UDim2.new(0, 0, 0, 0)
    Container.AutomaticCanvasSize = "Y"
    Container.ScrollBarThickness = 4
    
    local Layout = Instance.new("UIListLayout", Container)
    Layout.Padding = UDim.new(0, 12)

    -- Button X buat tutup
    local CloseBtn = Instance.new("TextButton", Head)
    CloseBtn.Size = UDim2.new(0, 25, 0, 25)
    CloseBtn.Position = UDim2.new(1, -32, 0, 7)
    CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    CloseBtn.Text = "X"
    CloseBtn.TextColor3 = Color3.new(1, 1, 1)
    CloseBtn.BorderSizePixel = 2
    CloseBtn.BorderColor3 = Color3.new(0, 0, 0)
    CloseBtn.MouseButton1Click:Connect(function() sg:Destroy() end)

    local Elements = {}

    -- [[ TOGGLE DENGAN SISTEM CENTANG & SILANG ]]
    function Elements:AddToggle(cfg)
        local toggled = false
        local tgl = Instance.new("TextButton", Container)
        tgl.Size = UDim2.new(1, 0, 0, 50)
        tgl.BackgroundColor3 = Color3.fromRGB(85, 230, 180)
        tgl.BorderSizePixel = 3
        tgl.BorderColor3 = Color3.new(0, 0, 0)
        tgl.Text = "  " .. tostring(cfg.Name)
        tgl.Font = Enum.Font.SourceSansBold
        tgl.TextSize = 18
        tgl.TextColor3 = Color3.new(0, 0, 0)
        tgl.TextXAlignment = "Left"
        
        -- Kotak Simbol di Kanan
        local indFrame = Instance.new("Frame", tgl)
        indFrame.Size = UDim2.new(0, 35, 0, 35)
        indFrame.Position = UDim2.new(1, -45, 0.5, -17)
        indFrame.BackgroundTransparency = 1 -- No Background
        indFrame.BorderSizePixel = 3
        indFrame.BorderColor3 = Color3.new(0, 0, 0)

        local symbol = Instance.new("TextLabel", indFrame)
        symbol.Size = UDim2.new(1, 0, 1, 0)
        symbol.BackgroundTransparency = 1
        symbol.Text = "X" -- Start dengan X (Off)
        symbol.Font = Enum.Font.SourceSansBold
        symbol.TextSize = 25
        symbol.TextColor3 = Color3.new(0, 0, 0)

        tgl.MouseButton1Click:Connect(function()
            toggled = not toggled
            cfg.Callback(toggled)
            symbol.Text = toggled and "✓" or "X"
            -- Opsional: Warna simbol berubah pas ON
            symbol.TextColor3 = toggled and Color3.fromRGB(0, 100, 0) or Color3.new(0, 0, 0)
        end)
    end

    -- [[ TEXTBOX PERSEGI PANJANG PUTIH ]]
    function Elements:AddTextbox(cfg)
        local boxFrame = Instance.new("Frame", Container)
        boxFrame.Size = UDim2.new(1, 0, 0, 50)
        boxFrame.BackgroundTransparency = 1
        
        local label = Instance.new("TextLabel", boxFrame)
        label.Text = "  " .. tostring(cfg.Name)
        label.Size = UDim2.new(0.5, 0, 1, 0)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.SourceSansBold
        label.TextSize = 18
        label.TextColor3 = Color3.new(0, 0, 0)
        label.TextXAlignment = "Left"

        local input = Instance.new("TextBox", boxFrame)
        input.Size = UDim2.new(0.4, 0, 0.7, 0)
        input.Position = UDim2.new(0.55, 0, 0.15, 0)
        input.Text = tostring(cfg.Default or "")
        input.BackgroundColor3 = Color3.new(1, 1, 1)
        input.BorderSizePixel = 3
        input.BorderColor3 = Color3.new(0, 0, 0)
        input.Font = Enum.Font.SourceSansBold
        input.TextSize = 16
        input.TextColor3 = Color3.new(0, 0, 0)
        input.FocusLost:Connect(function() cfg.Callback(input.Text) end)
    end

    return Elements
end

return ClearLib

